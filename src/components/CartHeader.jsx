function Header() {
  return (
    <header>
      <h1>React Cart</h1>
    </header>
  );
}
export default Header;

// 애플리케이션 전체에서 반복적으로 사용되는 상단 제목을 정의하는 컴포넌트

// function Header(): Header라는 이름의 함수형 컴포넌트를 정의합니다.
// return: 컴포넌트가 렌더링할 JSX를 반환합니다.
// <div>: HTML의 div 태그와 유사하며, 컴포넌트의 내용을 감싸는 역할
// export default Header: 이 컴포넌트를 다른 파일에서 사용할 수 있도록 내보냅니다.
